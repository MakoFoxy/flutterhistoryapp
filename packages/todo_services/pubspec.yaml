name: todo_services

description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.0.15
  cupertino_icons: ^1.0.2
  google_fonts: ^5.1.0 
  sqlite_bm25: ^0.3.0
  styled_text: ^8.1.0
  todo_repo:
    path: ../todo_repo

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
